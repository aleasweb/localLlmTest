# Генератор лабиринтов на PHP 8.3

Проект представляет собой генератор лабиринтов, написанный на PHP 8.3 с использованием алгоритма рекурсивного обхода с возвратом (Recursive Backtracking). Проект включает в себя генерацию лабиринтов, их визуализацию в различных форматах и проверку проходимости.

## 🎯 Цели проекта

Этот проект создан для опробования работы с локальной LLM через Ollama и сравнения различных моделей по скорости работы и качеству генерации.

## 🚀 Возможности

- **Генерация лабиринтов** с настраиваемыми размерами
- **Множественные форматы вывода**:
  - Текстовое представление в консоли
  - PNG изображения
  - Интерактивные HTML страницы
- **Проверка проходимости** лабиринта
- **Статистика** (количество стен, проходов, процент проходимости)
- **Настраиваемые цвета** и размеры ячеек

## 📋 Требования

- **PHP 8.3** или выше
- **Расширение GD** для создания изображений

### Установка зависимостей

#### Ubuntu/Debian:
```bash
sudo apt-get install php8.3-gd
```

#### macOS:
```bash
brew install php@8.3
```

#### Windows:
Убедитесь, что расширение GD включено в php.ini

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd maze
```

2. Установите зависимости через Composer:
```bash
composer install
```

## 🎯 Использование

### Быстрый старт

Запустите пример использования:

```bash
php example.php
```

Это создаст:
- Лабиринт размером 21x21
- Текстовое представление в консоли
- PNG изображение
- HTML файл для интерактивного просмотра

### Программное использование

```php
<?php

require_once 'vendor/autoload.php';

use Maze\MazeGenerator;
use Maze\MazeRenderer;

// Создание генератора (размеры должны быть нечетными)
$generator = new MazeGenerator(21, 21);
$maze = $generator->generate();

// Проверка проходимости
if ($generator->isSolvable()) {
    echo "Лабиринт проходим!\n";
}

// Создание рендерера
$renderer = new MazeRenderer($maze, 15);

// Текстовый вывод
echo $renderer->renderAsText();

// Создание изображения
$imageFile = $renderer->renderAsImage();

// Создание HTML
$htmlContent = $renderer->renderAsHtml();
file_put_contents('maze.html', $htmlContent);
```

## 📚 API Документация

### Класс MazeGenerator

#### Конструктор
```php
public function __construct(int $width, int $height)
```
Создает генератор лабиринта с указанными размерами.

**Параметры:**
- `$width` - ширина лабиринта (должна быть нечетной)
- `$height` - высота лабиринта (должна быть нечетной)

#### Методы

##### generate()
```php
public function generate(): array
```
Генерирует лабиринт и возвращает двумерный массив.

**Возвращает:** Массив, где `1` = стена, `0` = проход

##### isSolvable()
```php
public function isSolvable(): bool
```
Проверяет, есть ли путь от входа к выходу.

**Возвращает:** `true` если лабиринт проходим, `false` иначе

##### getDimensions()
```php
public function getDimensions(): array
```
Возвращает размеры лабиринта.

**Возвращает:** Массив с ключами `width` и `height`

### Класс MazeRenderer

#### Конструктор
```php
public function __construct(array $maze, int $cellSize = 20)
```
Создает рендерер для отображения лабиринта.

**Параметры:**
- `$maze` - двумерный массив лабиринта
- `$cellSize` - размер ячейки в пикселях (по умолчанию 20)

#### Методы

##### renderAsText()
```php
public function renderAsText(): string
```
Создает текстовое представление лабиринта.

**Возвращает:** Строка с ASCII-представлением лабиринта

**Символы:**
- `██` - стена
- `S ` - вход
- `E ` - выход
- `  ` - проход

##### renderAsImage()
```php
public function renderAsImage(string $filename = null): string
```
Создает PNG изображение лабиринта.

**Параметры:**
- `$filename` - имя файла (если не указано, генерируется автоматически)

**Возвращает:** Имя сохраненного файла

##### renderAsHtml()
```php
public function renderAsHtml(): string
```
Создает HTML представление лабиринта с CSS стилями.

**Возвращает:** HTML строка с интерактивным лабиринтом

##### setCellSize()
```php
public function setCellSize(int $cellSize): void
```
Устанавливает размер ячейки.

##### setColors()
```php
public function setColors(array $colors): void
```
Настраивает цвета для отображения.

**Параметры:**
- `$colors` - массив с ключами: `wall`, `path`, `start`, `end`, `border`

## 🎨 Настройка внешнего вида

### Изменение цветов

```php
$renderer->setColors([
    'wall' => [0, 0, 0],      // Черный для стен
    'path' => [255, 255, 255], // Белый для проходов
    'start' => [0, 255, 0],    // Зеленый для входа
    'end' => [255, 0, 0],      // Красный для выхода
    'border' => [128, 128, 128] // Серый для границ
]);
```

### Изменение размера ячеек

```php
$renderer->setCellSize(25); // Увеличить размер ячейки
```

## 🔧 Алгоритм генерации

Проект использует алгоритм **рекурсивного обхода с возвратом**:

1. **Инициализация**: Создается сетка, где все ячейки являются стенами
2. **Начальная точка**: Выбирается начальная ячейка (обычно 1,1)
3. **Рекурсивный обход**: 
   - Помечаем текущую ячейку как посещенную
   - Случайно выбираем направление движения
   - Если соседняя ячейка не посещена, пробиваем стену между ними
   - Рекурсивно продолжаем с новой ячейки
4. **Возврат**: Если нет доступных направлений, возвращаемся назад
5. **Завершение**: Когда все ячейки посещены, устанавливаем вход и выход

## 📊 Статистика

При запуске `example.php` выводится статистика лабиринта:
- Размеры лабиринта
- Количество стен и проходов
- Процент проходимости
- Результат проверки проходимости

## 🎮 Интерактивное использование

Созданный HTML файл можно открыть в браузере для интерактивного просмотра лабиринта с цветовой легендой.

## 🐛 Устранение неполадок

### Ошибка "Требуется PHP 8.3 или выше"
Убедитесь, что у вас установлена правильная версия PHP:
```bash
php --version
```

### Ошибка "Требуется расширение GD"
Установите расширение GD для вашей системы (см. раздел "Установка зависимостей").

### Лабиринт не генерируется
Убедитесь, что размеры лабиринта являются нечетными числами.

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## 🤝 Вклад в проект

Приветствуются вклады в виде:
- Исправления ошибок
- Новые алгоритмы генерации
- Дополнительные форматы вывода
- Улучшения документации

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории проекта или обратитесь к автору по email: maze@example.com
